extends base

block content 
    if errorMessage
        span=errorMessage
    form(method="post")
        input(name="title",placeholder="Title", required, type="text")
        input(type="text" name="summary",placeholder="summary",required)     
        input(type="text" ,name="geners",placeholder="genersm separated by comma.",required)
        input(type="submit", value="Upload Video")