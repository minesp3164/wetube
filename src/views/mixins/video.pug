mixin video(video)
    div 
     h4
        a(href=`/videos/${video.id}`)=video.title
    p=video.summary
    ul  
        each geners in video.geners
            li=geners
    small=video.year
    hr


         